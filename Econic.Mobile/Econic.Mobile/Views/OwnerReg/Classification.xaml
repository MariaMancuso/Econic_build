<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Econic.Mobile.Views.OwnerReg.Classification"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="WhiteSmoke"
             ControlTemplate="{StaticResource NavigationTemplate}"
             xmlns:input="clr-namespace:Plugin.InputKit.Shared.Controls;assembly=Plugin.InputKit">
    <ContentPage.Content>
        <Grid RowSpacing="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            
            <Label Grid.Row="0" Text="This is where you upload 5 to 10 of your bestselling products and services." FontFamily="QuicksandBold" TextColor="#404040" FontSize="20"/>
            <StackLayout Grid.Row="1">
                <Label Padding="0, 0, 10, 0" Text="We're all about profits so we only care about the bestsellers, you should too." FontFamily="QuicksandBold" TextColor="#404040"/>
                <Label Text="Select all the apply." FontFamily="QuicksandBold" TextColor="#404040"/>
            </StackLayout>

            <StackLayout Grid.Row="2" Orientation="Vertical">

                <ListView ItemSelected="ItemSelected" x:Name="CMlistView" Footer="">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Frame HasShadow="True" WidthRequest="70" HorizontalOptions="Fill" CornerRadius="20" Margin="0,0, 0, 5" BackgroundColor="White">
                                    <StackLayout Orientation="Horizontal">
                                        <Image Source="{Binding Name}" WidthRequest="30" HeightRequest="30"/>
                                        <Label Text="{Binding Name}" VerticalTextAlignment="Center" TextColor="#404040" FontFamily="QuicksandBold" FontSize="16"/>
                                    </StackLayout>
                                </Frame>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Label Text="Have a picture, name, price range and description prepared." Margin="0, 10, 0, 0" TextColor="#404040" FontFamily="QuicksandBold"/>
            </StackLayout>


            <StackLayout Grid.Row="3" VerticalOptions="End">
                <Button Clicked="ContinueClicked" Text="Next" Style="{StaticResource NextItemButton}"/>
                <ContentView ControlTemplate="{StaticResource Footer}"/>
            </StackLayout>
            
        </Grid>
    </ContentPage.Content>
</ContentPage>