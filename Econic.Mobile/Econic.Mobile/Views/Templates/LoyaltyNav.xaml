<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Econic.Mobile.Views.Templates.LoyaltyNav"
             Padding="0">
    <ContentView.Content>
        <Grid VerticalOptions="FillAndExpand"
                  Padding="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" VerticalOptions="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <ImageButton Grid.Column="0" Source="backarrow" HorizontalOptions="Start"
                                 BackgroundColor="Transparent"
                                 WidthRequest="25"
                                 HeightRequest="25"/>
                <!--Clicked="OnBackButtonPressed" />-->
                <Label Grid.Column="1" Text="REVYVV"
                           HorizontalOptions="CenterAndExpand"
                           TextColor="Silver"
                           FontSize="14"/>

            </Grid>
        </Grid>
    </ContentView.Content>
</ContentView>